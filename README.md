# Employee-Perfomance-Analysis
Employee Performance Dashboard built in SQL Server — uses window functions (RANK, NTILE, PARTITION BY) on real HR data to rank salaries by department, flag pay outliers, and calculate attrition by role.
